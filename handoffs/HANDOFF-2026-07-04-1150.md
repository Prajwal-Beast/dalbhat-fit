# Session Handoff — DhalBhat Fit production-readiness QA & release blockers fixed · 2026-07-04 11:50

## 1. Goal / what we're building
DhalBhat Fit — Nepal-first diet & fitness Flutter app (Nepali foods, home/gym
workouts, weight tracking). Owner: Prajwal (prajwalbeast22@gmail.com), package
`com.dhalbhatfit.dalbhat_fit`, on Play closed testing. **Session goal (user's
/goal, still active):** make the app completely production ready with everything
running smoothly, verified end-to-end by actually driving the app — no
hallucinated claims; every fix proven with evidence.

## 2. Current state (where we are right now)
- ✅ **THE historic bug found & fixed: missing INTERNET permission** in
  `AndroidManifest.xml`. Every RELEASE build ever made had zero network access
  (debug builds auto-inject the permission, masking it). ✓ Verified by
  `aapt dump permissions` (before: no INTERNET; after: present) AND live: signup
  from the real phone created `dhalbhatqa20260626@gmail.com` in `auth.users`
  (confirmed, session active) — first successful release-build network call ever.
- ✅ Food DB accuracy fixed via migration `fix_per100g_semantics_and_seed_portions`
  (~12 foods had per-PIECE kcal in the per-100g column; momo 8 pcs was 96 kcal,
  now 420). ✓ Verified by SQL re-query of foods + portions.
- ✅ Fonts bundled (`app/assets/google_fonts/`), runtime fetching disabled.
  ✓ `flutter analyze` clean; APK builds.
- ✅ Version bumped to **1.0.1+2**; fresh APK (63.8MB) and **AAB (61.8MB) built —
  the AAB must be uploaded to Play Console** (the 1.0.0+1 AAB there is dead).
- ✅ Supabase project was found **INACTIVE (free-tier auto-pause)** — restored via
  MCP. Advisor fix applied: `set_search_path_on_set_updated_at`.
- ✅ All work committed & pushed to GitHub `master` (see §5).
- ⏳ **On-device E2E incomplete** — blocked on flaky USB (phone disconnected).
  Done: signup ✓ (server-verified). Remaining: onboarding → calorie-goal screen
  (must show **1920 kcal** with defaults), food logging vs corrected DB values,
  long-press delete-meal, workout session completion + streak, weight/progress,
  settings persistence, sign-out/sign-in, Google login on device.
- ⏳ Final flaw report + ship/no-ship verdict not yet written (task #12).

## 3. Decisions made (with rationale)
| Decision | Choice | Why |
|---|---|---|
| Dark mode | Deferred; `themeMode: ThemeMode.light` forced; toggle says "coming soon" but persists | Screens hard-code light `AppColors` (57+ refs); real dark needs palette migration; user approved deferral |
| Units | Store kg ALWAYS; convert only at display/input (`core/utils/units.dart`) | Single source of truth; no drift in stored data |
| Food data fix | Fix per-100g values + rename foods + seed piece portions in DB (no app code change) | Data was objectively wrong (momo 4× under); portions also fix the null `portion_id` path |
| Fonts | Bundle in assets, `allowRuntimeFetching=false` | Offline-first app for Nepal networks; no font flash; fewer runtime deps |
| Testing device | Real phone over emulator | Emulator app-level DNS is broken on this PC (TAP adapter); phone is ground truth |
| Version | 1.0.1+2 | Play requires increasing build number; 1.0.0+1 AAB is non-functional |

## 4. Files created / changed (this session)
| Path | What it is | Status |
|---|---|---|
| `app/android/app/src/main/AndroidManifest.xml` | +INTERNET permission (with warning comment) | edited, committed |
| `app/pubspec.yaml` | version 1.0.1+2; +assets/google_fonts/ | edited, committed |
| `app/lib/main.dart` | `GoogleFonts.config.allowRuntimeFetching=false` + OFL LicenseRegistry | edited, committed |
| `app/assets/google_fonts/*` | Poppins (Light→ExtraBold), NotoSansDevanagari (Reg+Medium), OFL.txt | created, committed |
| `HANDOFF.md` | 2026-07-04 session entry | edited, committed |
| `C:\Users\TUF\.claude\skills\session-handoff\SKILL.md` | upgraded with completeness checklist | edited (outside repo) |
| `C:\Users\TUF\.claude\projects\C--claude-code-dalbhat-fit\memory\project_dhalbhat_fit.md` | status refreshed | edited (outside repo) |

## 5. Git & release state
- Branch: `master`, **fully pushed** to `https://github.com/Prajwal-Beast/dalbhat-fit`.
- Today's commits (newest first): `c035438` HANDOFF entry · `2082a08` bundle fonts ·
  `7d12099` version 1.0.1+2 · `4d39e1d` INTERNET permission · (2026-06-26 batch:
  `7f538a2` handoff, `f89401a` settings/units/delete-meal, `bedab49` portion_id+auth fixes).
- Working tree: clean at time of writing (verify with `git status`).
- Artifacts (both include ALL fixes, v1.0.1+2):
  - APK: `app/build/app/outputs/flutter-apk/app-release.apk` (63.8MB) — installed on phone
  - AAB: `app/build/app/outputs/bundle/release/app-release.aab` (61.8MB) — **UPLOAD TO PLAY**
- Play status: closed testing; needs 12 testers × 14 days; current uploaded AAB is broken.

## 6. Backend / server-side & external state
- Supabase project **hmjqotxfdmirbomfhrgu** (DhalBhat Fit), region ap-south-1.
  Was INACTIVE (auto-paused) → restored this session. **Free tier re-pauses after
  ~7 days idle** — check `get_project` status FIRST when "connection" errors appear.
- Migrations applied this session: `fix_per100g_semantics_and_seed_portions`
  (foods/portions/templates data fix), `set_search_path_on_set_updated_at`.
- DB now: 50 foods (renamed, corrected), portions seeded for ~15 foods (momo 4/8/12
  pcs, chapati 1/2/3, egg 1/2, wai wai pack, etc.), momo template 440 kcal.
- Remaining advisors: Leaked Password Protection OFF (user dashboard toggle);
  `beast-media` public bucket listing (looks unrelated to this app — confirm).
- Google OAuth: provider live (`/auth/v1/authorize?provider=google` → 302 to Google,
  client id 8699…, callback correct). ⚠️ UNVERIFIED: Redirect URLs allowlist must
  contain `com.dhalbhatfit.dalbhat_fit://login-callback/` — check in dashboard.
- Auth users: owner `prajwalbeast22@gmail.com` (never signed in! confirmed=false),
  QA `dhalbhatqa20260626@gmail.com` (created 2026-07-04, confirmed, signed in ✓).

## 7. Tools, MCPs & integrations used
- **Supabase MCP connector `2e145209-4c8d-4592-a0a7-f85bcfb4e541` = CORRECT project.**
  The plain `supabase` MCP points at a WRONG project (rpeqnasynmotozsegijh) — never
  use it for DhalBhat DB work.
- adb at `$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe`; aapt under
  `...\Android\Sdk\build-tools\<ver>\aapt.exe` (used for permission dumps).

## 8. Environment, devices & working commands
- Phone: id `EIQWTOFQJV8DYPSS`, screen **720x1600** (1:1 tap coords from screenshots).
  USB is FLAKY (~1 min before drop). Plan: as soon as it's connected run
  `adb tcpip 5555` + `adb connect <phone-ip>:5555` to survive cable drops.
  Screenshot pattern (PowerShell `>` corrupts binary!):
  `adb -s EIQWTOFQJV8DYPSS shell screencap -p /sdcard/s.png; adb -s ... pull /sdcard/s.png <dest>`
- Emulator AVD "Phone": **app-level DNS is broken on this PC** — do NOT use for
  network testing. Failed workarounds (don't retry): `-dns-server 8.8.8.8,1.1.1.1`,
  `settings put global private_dns_mode off`, airplane-mode toggle, `svc wifi disable`
  (forces cellular, still dead), `adb root` (production image, refused). Suspected
  cause: TAP-Windows (OpenVPN) adapter hijacking qemu DNS. Chrome-in-emulator works
  (own DoH) — misleading.
- Build (MUST include env file): `flutter build apk --release --dart-define-from-file=.env.local`
  (AAB: `flutter build appbundle --release --dart-define-from-file=.env.local`), run in `app/`.
- Secrets locations (NEVER commit/paste): `app/.env.local` (SUPABASE_URL/ANON_KEY),
  `app/android/key.properties`, `app/android/dalbhat-fit-release.jks`. All git-ignored.
- QA login: `dhalbhatqa20260626@gmail.com` / `TestPass123!` (throwaway test account).
- adb text entry: spaces = `%s` (e.g. `input text 'QA%sTester'`); Nepali "Hamro Patro"
  keyboard may pop up — `input text` still works; close keyboard with `keyevent 111`
  or `keyevent 4`.

## 9. Key facts & data the next session must know
- **Expected test values** (verify in-app): onboarding with ALL DEFAULTS (name only)
  = male/25y/165cm/65kg/moderate/lose → BMR 1561.25, TDEE 2419.9, **target 1920 kcal**,
  BMI 23.9 → "overweight" (Asian cutoff 23.0). Foods: momo steamed pork 8 pcs
  **420 kcal**, chapati 1 pc **104**, dal bhat medium thali **715**, egg 1 pc **78**,
  Wai Wai pack **321**. Dashboard totals must match portion kcal exactly.
- Calorie engine `app/lib/core/utils/calorie_calculator.dart` verified correct
  (Mifflin-St Jeor; lose=−500 clamp ≥800; gain=+300; Asian BMI).
- Supabase rejects `@example.com` emails; gmail-format works (confirmation is OFF).
- `onboarding_screen.dart:92` saves profile fire-and-forget → if save fails user
  still lands on calorie screen (watch during E2E; potential silent-failure flaw).
- Deferred/known-open (not release blockers): dark mode migration, weight offline-
  sync dead code, food-search debounce, offline-delete tombstone, workout videos
  (15 exercises listed in `docs/workout-videos-needed.md`), photo-log tab "soon",
  Sentry re-add, Hypertrophy day-2 has only 2 exercises.

## 10. Known traps & time-wasters (don't repeat these)
- "Check your connection" in release builds was the missing INTERNET permission —
  fixed in `4d39e1d`; NEVER remove it. If it recurs, `aapt dump permissions` first.
- Supabase free tier auto-pauses → app looks broken; check project status before
  debugging anything else.
- Emulator on this PC: app DNS dead (see §8) — use the real phone.
- PowerShell `... > file.png` corrupts adb binary output (UTF-16) — use screencap-
  to-device + pull.
- Play-signed vs local-signed APKs: signature mismatch → `adb uninstall` first
  (wipes local Drift data; remote data survives).
- Old `supabase` MCP = wrong project; use the 2e145209 connector.

## 11. Workflow & conventions established
- HANDOFF.md at repo root = append-only dated session log; READ FIRST each session.
- Council mindset: don't yes-man; verify before claiming; every "fixed" needs proof.
- Git: branch off master → commit → ff-merge → push; commit messages end with
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`; never bypass hooks.
- Security: Claude API key only in Supabase Edge Function env; never commit
  `.env.local`/`*.jks`/`key.properties`.
- Store kg; convert at UI. Deterministic local calorie math (Claude only identifies
  food, never calculates).

## 12. Open tasks / next steps
1. **Reconnect phone** → `adb tcpip 5555` + WiFi connect (survive USB drops).
2. Install `app-release.apk` (has fonts; phone currently runs the INTERNET-only build).
3. E2E: onboarding (defaults; assert **1920 kcal** on plan screen) → verify
   `user_profiles` row in DB.
4. E2E: log foods (momo 8 pcs → 420; chapati 1 pc → 104); dashboard total matches;
   `food_logs` rows in DB with correct `portion_id`; kill+relaunch → persists.
5. E2E: long-press logged meal slot → delete a log → totals update; DB row gone.
6. E2E: start Home Full Body plan → complete sets → finish → `workout_sessions`
   row + streak=1; repeat-day guard.
7. E2E: log weight; Progress chart + goal bar move; toggle lbs → all weights
   convert; toggle settings → force-stop → relaunch → persisted.
8. E2E: sign out → sign in; Google login tap (expect browser → account → back
   into app; requires Redirect URLs allowlist).
9. Write final flaw report + ship/no-ship (task #12); append results to HANDOFF.md.
10. USER ACTIONS: upload new AAB (1.0.1+2) to Play closed testing; verify Redirect
    URLs allowlist; enable Leaked Password Protection; recruit 12 testers; decide
    free-tier keep-alive plan.
- Background at handoff time: device-poll task `b6ykn2hix` (15 min) + scheduled
  wakeup ~12:07 carrying the full E2E loop prompt; /goal Stop-hook active
  ("completely production ready… /loop").

## 13. ▶️ Resume here
Run `adb devices`; when `EIQWTOFQJV8DYPSS` shows `device`, immediately switch to
WiFi adb (`adb tcpip 5555`, get IP from `adb shell ip route`, `adb connect <ip>:5555`),
install `app/build/app/outputs/flutter-apk/app-release.apk`, then execute steps 3–9
of §12 in order, screenshotting each step and verifying against the expected values
in §9 and live Supabase (connector 2e145209). Then write the flaw report and update
HANDOFF.md.

## 14. Pointers
- Read first: `C:\claude code\dalbhat-fit\HANDOFF.md` (full dated log)
- Memory: `C:\Users\TUF\.claude\projects\C--claude-code-dalbhat-fit\memory\project_dhalbhat_fit.md`
- Specs: `C:\claude code\dalbhat-fit\docs\`
- QA screenshots this session: `C:\Users\TUF\AppData\Local\Temp\claude\C--claude-code-dalbhat-fit\fb462f87-abdc-461a-a9b9-f28de0378a9c\scratchpad\` (p01–p12, s01–s15)
- Transcript: `C:\Users\TUF\.claude\projects\C--claude-code-dalbhat-fit\fb462f87-abdc-461a-a9b9-f28de0378a9c.jsonl`
